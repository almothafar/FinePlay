package custom.core.module;

import com.google.inject.AbstractModule;

public class UserAuthenticatorModule extends AbstractModule {

	@Override
	protected void configure() {

		// bind(UserAuthenticator.class).to(AnotherUserAuthenticator.class);
	}
}
